# Métodos em tuplas

cores = ("vermelho", "azul", "verde", "azul",)

# Contando o número que tal item aparece na tupla
print(cores.count("vermelho"))
print(cores.count("azul"))

# Retornando a posição do item
print(cores.index("verde"))

# Retornando o tamanho total da tupla
print(len(cores))
